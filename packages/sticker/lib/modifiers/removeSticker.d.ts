/**
 * Removes a sticker from the editor state
 */
import { EditorState } from 'draft-js';
declare const _default: (editorState: EditorState, blockKey: string) => EditorState;
export default _default;
